<div class="row">
  <div class="col-md-3">
    <ul class="sidebar-list">
      <li class="sidebar-item"><%= link_to "Students", students_path, class: "sidebar-link active" %></li>
    </ul>
  </div>
  <div class="col-md-9">
    <div class="panel panel-primary">
      <div class="panel-heading">
        Students
      </div>
      <div class="panel-body">

        <% @students.each do |student| %>
        <div class="row">
          <div class="col-md-7">
            <h4><%= student.listing_name %></h4>
          </div>
          <div class="col-md-3 right">
            <%= link_to "View", student, class: "btn btn-default" %>
            <%= link_to "Update", edit_student_path(s), class: "btn btn-default" %>
          </div>
        </div>
        <% end %>

      </div>
    </div>

    <%= link_to "Add new place", new_student_path, class: "btn btn-primary" %>
  </div>
</div>
